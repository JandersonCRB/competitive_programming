#include <stdio.h>

main()
{
    int d,x,y,z;
    scanf("%d",&d);
    scanf("%d%d%d",&x,&y,&z);
    if(x>=d&&y>=d&&x>=d)
    {
        printf("S");
    }
    else
    {
        printf("N");
    }
}
